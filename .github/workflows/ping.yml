name: ping
on:
  push:
    branches: ["claude/**"]
  workflow_dispatch:
jobs:
  hello:
    runs-on: ubuntu-latest
    steps:
      - run: echo "hello from $(uname -a)"
      - run: node --version || true
      - run: npm --version || true
